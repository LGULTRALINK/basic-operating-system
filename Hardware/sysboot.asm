	MOV	AX,PUSH	[AH]	POP	[AL] ;	accumulator
	MOV	BX,PUSH	[BH]	POP	[BL] ;	base record
	MOV	CX, ;	counter
	MOV	DX, ;	data
	MOV	CS, ;	code segment
	MOV	DS, ;	data segment
	MOV	ES, ;	extra segment
	MOV	SS, ;	stack segment
	MOV	IP, ;	program index
	MOV	SI, ;	origin index
	MOV	DI, ;	address index
	MOV	SP, ;	stack index
	MOV	BP, ;	base index
